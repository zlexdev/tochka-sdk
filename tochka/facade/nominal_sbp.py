# AUTO-GENERATED by tochka.codegen — regenerate, do not hand-edit.
"""СБП — client facade mixin (auto-generated from the Tochka OpenAPI spec)."""

from __future__ import annotations

from typing import Any

from ..methods.nominal_sbp import GenerateSbpQrcodeV2, ListBankSbpV2
from ..models.nominal_sbp import GenerateSbpQrcodeV2Params
from ._base import FacadeBase


class NominalSbpFacade(FacadeBase):
    """``Client`` mixin — СБП endpoints."""

    async def generate_sbp_qrcode_v2(
        self,
        id: Any,
        jsonrpc: str,
        method: str,
        amount: Any,
        purpose: str,
        height: int = 300,
        merchant_id: str | None = None,
        nominal_bic: str | None = None,
        nominal_code: str | None = None,
        source_name: str | None = None,
        width: int = 300,
    ) -> None:
        """Генерация QR-кода для пополнения номинального счёта через СБП. Метод включается по запросу. Если не
        передать реквизиты номинального счёта, будет использован счёт по умолчанию. via ``POST
        /v2/jsonrpc/generate_sbp_qrcode``.

        Args:
            id: JsonRPC id
            amount: Сумма пополнения
            height: Высота изображения
            nominal_bic: БИК номинального счёта
            nominal_code: Номер номинального счёта
            purpose: Дополнительная информация от ТСП по платежу (в платеже будет указано стандартное
                назначение)
            width: Ширина изображения
        """
        return await self.execute(
            GenerateSbpQrcodeV2(
                id=id,
                jsonrpc=jsonrpc,
                method=method,
                params=GenerateSbpQrcodeV2Params(
                    amount=amount,
                    height=height,
                    merchant_id=merchant_id,
                    nominal_bic=nominal_bic,
                    nominal_code=nominal_code,
                    purpose=purpose,
                    source_name=source_name,
                    width=width,
                ),
            )
        )

    async def list_bank_sbp_v2(
        self, id: Any, jsonrpc: str, method: str, params: dict[str, Any] | None = None
    ) -> None:
        """Получение списка банков-участников СБП с информацией по ним via ``POST /v2/jsonrpc/list_bank_sbp``.

        Args:
            id: JsonRPC id
        """
        return await self.execute(ListBankSbpV2(id=id, jsonrpc=jsonrpc, method=method, params=params))
