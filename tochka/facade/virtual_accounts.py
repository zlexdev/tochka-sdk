# AUTO-GENERATED by tochka.codegen — regenerate, do not hand-edit.
"""Виртуальные счета — client facade mixin (auto-generated from the Tochka OpenAPI spec)."""

from __future__ import annotations

from typing import Any

from ..enums.virtual_accounts import CreateVirtualAccountV2ParamsVirtualAccountType
from ..methods.virtual_accounts import (
    CreateVirtualAccountV2,
    GetVirtualAccountsTransferV2,
    GetVirtualAccountV2,
    ListVirtualAccountV2,
    ListVirtualTransactionV2,
    RefundVirtualAccountV2,
    TransferBetweenVirtualAccountsV2,
    TransferBetweenVirtualAccountsV2V2,
)
from ..models.virtual_accounts import (
    CreateVirtualAccountV2Params,
    GetVirtualAccountsTransferV2Params,
    GetVirtualAccountV2Params,
    ListVirtualAccountV2Params,
    ListVirtualAccountV2ParamsFilters,
    ListVirtualTransactionV2Params,
    ListVirtualTransactionV2ParamsFilters,
    RefundVirtualAccountV2Params,
    RefundVirtualAccountV2ParamsRecipient,
    TransferBetweenVirtualAccountsV2Params,
    TransferBetweenVirtualAccountsV2V2Params,
)
from ._base import FacadeBase


class VirtualAccountsFacade(FacadeBase):
    """``Client`` mixin — Виртуальные счета endpoints."""

    async def create_virtual_account_v2(
        self,
        id: Any,
        jsonrpc: str,
        method: str,
        beneficiary_id: str | None = None,
        inn: str | None = None,
        nominal_account_bic: str | None = None,
        nominal_account_code: str | None = None,
        virtual_account_type: CreateVirtualAccountV2ParamsVirtualAccountType = CreateVirtualAccountV2ParamsVirtualAccountType.STANDARD,
    ) -> None:
        """Метод создаёт виртуальный счет для бенефициара. via ``POST /v2/jsonrpc/create_virtual_account``.

        Args:
            id: JsonRPC id
            beneficiary_id: Идентификатор бенефициара
            inn: 10 или 12 цифр, по реквизитам из банка; Необязательный для счетов, начинающихся на 40817,
                423, 40820, 40803, 40813, 426
            nominal_account_bic: БИК номинального счёта
            nominal_account_code: Номер номинального счёта
            virtual_account_type: Типы виртуальных счетов.
        """
        return await self.execute(
            CreateVirtualAccountV2(
                id=id,
                jsonrpc=jsonrpc,
                method=method,
                params=CreateVirtualAccountV2Params(
                    beneficiary_id=beneficiary_id,
                    inn=inn,
                    nominal_account_bic=nominal_account_bic,
                    nominal_account_code=nominal_account_code,
                    virtual_account_type=virtual_account_type,
                ),
            )
        )

    async def get_virtual_account_v2(self, id: Any, jsonrpc: str, method: str, virtual_account: str) -> None:
        """Метод возвращает информацию по выбранному виртуальному счёту via ``POST
        /v2/jsonrpc/get_virtual_account``.

        Args:
            id: JsonRPC id
            virtual_account: Уникальный идентификатор виртуального счёта
        """
        return await self.execute(
            GetVirtualAccountV2(
                id=id,
                jsonrpc=jsonrpc,
                method=method,
                params=GetVirtualAccountV2Params(virtual_account=virtual_account),
            )
        )

    async def get_virtual_accounts_transfer_v2(
        self, id: Any, jsonrpc: str, method: str, transfer_id: str
    ) -> None:
        """Метод возвращает информацию о выбранном переводе между виртуальными счётами via ``POST
        /v2/jsonrpc/get_virtual_accounts_transfer``.

        Args:
            id: JsonRPC id
            transfer_id: Идентификатор перевода между виртуальными счетами
        """
        return await self.execute(
            GetVirtualAccountsTransferV2(
                id=id,
                jsonrpc=jsonrpc,
                method=method,
                params=GetVirtualAccountsTransferV2Params(transfer_id=transfer_id),
            )
        )

    async def list_virtual_account_v2(
        self,
        id: Any,
        jsonrpc: str,
        method: str,
        filters: ListVirtualAccountV2ParamsFilters | None = None,
        page: int = 1,
        per_page: int = 100,
    ) -> None:
        """Метод возвращает список виртуальных счетов, соответствующих заданным фильтрам via ``POST
        /v2/jsonrpc/list_virtual_account``.

        Args:
            id: JsonRPC id
            filters: Фильтры
            page: Страница
            per_page: Количество элементов на странице
        """
        return await self.execute(
            ListVirtualAccountV2(
                id=id,
                jsonrpc=jsonrpc,
                method=method,
                params=ListVirtualAccountV2Params(filters=filters, page=page, per_page=per_page),
            )
        )

    async def list_virtual_transaction_v2(
        self,
        id: Any,
        jsonrpc: str,
        method: str,
        filters: ListVirtualTransactionV2ParamsFilters | None = None,
        page: int = 1,
        per_page: int = 100,
    ) -> None:
        """Метод возвращает движение денег у выбранного виртуального счёта по заданным фильтрам via ``POST
        /v2/jsonrpc/list_virtual_transaction``.

        Args:
            id: JsonRPC id
            filters: Фильтры
            page: Страница
            per_page: Количество элементов на странице
        """
        return await self.execute(
            ListVirtualTransactionV2(
                id=id,
                jsonrpc=jsonrpc,
                method=method,
                params=ListVirtualTransactionV2Params(filters=filters, page=page, per_page=per_page),
            )
        )

    async def refund_virtual_account_v2(
        self,
        id: Any,
        jsonrpc: str,
        method: str,
        recipient: RefundVirtualAccountV2ParamsRecipient,
        virtual_account: str,
        ext_key: str | None = None,
        identifier: str | None = None,
        purpose: str | None = None,
    ) -> None:
        """Метод поможет вывести идентифицированные деньги с виртуального счёта. Вывод по реквизитам.
        Документы при такой операции подгружать не нужно. Метод нужен, например, для вывода
        ошибочно-зачисленных денежных средств. При добавлении ext_key в запрос метод станет идемпотентным.
        В случае повторного запроса с одним и тем же ext_key будет возвращен ответ оригинального запроса.
        via ``POST /v2/jsonrpc/refund_virtual_account``.

        Args:
            id: JsonRPC id
            ext_key: Внешний уникальный идентификатор операции для идемпотентности
            identifier: Уникальный идентификатор получателя
            purpose: Назначение платежа
            recipient: Реквизиты получателя
            virtual_account: Виртуальный счёт бенефициара, типа standard
        """
        return await self.execute(
            RefundVirtualAccountV2(
                id=id,
                jsonrpc=jsonrpc,
                method=method,
                params=RefundVirtualAccountV2Params(
                    ext_key=ext_key,
                    identifier=identifier,
                    purpose=purpose,
                    recipient=recipient,
                    virtual_account=virtual_account,
                ),
            )
        )

    async def transfer_between_virtual_accounts_v2(
        self,
        id: Any,
        jsonrpc: str,
        method: str,
        amount: float,
        from_virtual_account: str,
        to_virtual_account: str,
    ) -> None:
        """Метод используется для перевода денег в рамках одного номинального счёта. Может быть использован
        только для перевода денег в рамках одного номинального счёта. via ``POST
        /v2/jsonrpc/transfer_between_virtual_accounts``.

        Args:
            id: JsonRPC id
            amount: Сумма перевода
            from_virtual_account: Виртуальный счёт с которого списать
            to_virtual_account: Виртуальный счёт на который перевести
        """
        return await self.execute(
            TransferBetweenVirtualAccountsV2(
                id=id,
                jsonrpc=jsonrpc,
                method=method,
                params=TransferBetweenVirtualAccountsV2Params(
                    amount=amount,
                    from_virtual_account=from_virtual_account,
                    to_virtual_account=to_virtual_account,
                ),
            )
        )

    async def transfer_between_virtual_accounts_v2_v2(
        self,
        id: Any,
        jsonrpc: str,
        method: str,
        amount: float,
        from_virtual_account: str,
        to_virtual_account: str,
        ext_key: str | None = None,
        purpose: str | None = None,
    ) -> None:
        """Данный метод был реализован для целей миграции с одного банка на другой. Сейчас неактуален. Но всё
        ещё может использоваться для переводов в рамках одного номинального счета. Метод переводит деньги
        между виртуальными счетами. Если счета находятся на одном номинальном счёте, то перевод происходит
        мгновенно. В противном случае перевод осуществляется с помощью банковского платежа между
        номинальными счетами и завершается, когда платёж исполнен. Платёж идентифицируется автоматически
        согласно информации в запросе. Для получения статуса перевода необходимо использовать метод
        get_virtual_accounts_transfer. via ``POST /v2/jsonrpc/transfer_between_virtual_accounts_v2``.

        Args:
            id: JsonRPC id
            amount: Сумма перевода
            ext_key: Ключ идемпотентности
            from_virtual_account: Виртуальный счёт с которого списать
            purpose: Назначение платежа
            to_virtual_account: Виртуальный счёт на который перевести
        """
        return await self.execute(
            TransferBetweenVirtualAccountsV2V2(
                id=id,
                jsonrpc=jsonrpc,
                method=method,
                params=TransferBetweenVirtualAccountsV2V2Params(
                    amount=amount,
                    ext_key=ext_key,
                    from_virtual_account=from_virtual_account,
                    purpose=purpose,
                    to_virtual_account=to_virtual_account,
                ),
            )
        )
