# AUTO-GENERATED by tochka.codegen — regenerate, do not hand-edit.
"""Сделки — client facade mixin (auto-generated from the Tochka OpenAPI spec)."""

from __future__ import annotations

from typing import Any

from ..enums.deals import ListDealsV2ParamsFieldNames
from ..methods.deals import (
    CancelDealWithExecutedRecipientsV2,
    ComplianceCheckDealV2,
    CreateDealV2,
    ExecuteDealV2,
    GetDealV2,
    ListDealsV2,
    ListDealV2,
    RejectedDealV2,
    UpdateDealV2,
)
from ..models.deals import (
    CancelDealWithExecutedRecipientsV2Params,
    ComplianceCheckDealV2Params,
    CreateDealV2Params,
    CreateDealV2ParamsPayers,
    ExecuteDealV2Params,
    ExecuteDealV2ParamsRecipientsExecute,
    GetDealV2Params,
    ListDealsV2Params,
    ListDealsV2ParamsFilters,
    ListDealV2Params,
    ListDealV2ParamsFilters,
    RejectedDealV2Params,
    UpdateDealV2Params,
    UpdateDealV2ParamsDealData,
)
from ._base import FacadeBase


class DealsFacade(FacadeBase):
    """``Client`` mixin — Сделки endpoints."""

    async def cancel_deal_with_executed_recipients_v2(
        self, id: Any, jsonrpc: str, method: str, deal_id: str
    ) -> None:
        """Метод отменяет сделку, которая имеет статус correction и у неё только 1 плательщик via ``POST
        /v2/jsonrpc/cancel_deal_with_executed_recipients``.

        Args:
            id: JsonRPC id
            deal_id: Уникальный идентификатор сделки
        """
        return await self.execute(
            CancelDealWithExecutedRecipientsV2(
                id=id,
                jsonrpc=jsonrpc,
                method=method,
                params=CancelDealWithExecutedRecipientsV2Params(deal_id=deal_id),
            )
        )

    async def compliance_check_deal_v2(self, id: Any, jsonrpc: str, method: str, deal_id: str) -> None:
        """Метод проверяет платежи типа payment_contract и ndfl на соответствие требованиям законодательства
        via ``POST /v2/jsonrpc/compliance_check_deal``.

        Args:
            id: JsonRPC id
            deal_id: Уникальный идентификатор сделки
        """
        return await self.execute(
            ComplianceCheckDealV2(
                id=id, jsonrpc=jsonrpc, method=method, params=ComplianceCheckDealV2Params(deal_id=deal_id)
            )
        )

    async def create_deal_v2(
        self,
        id: Any,
        jsonrpc: str,
        method: str,
        amount: float,
        payers: list[CreateDealV2ParamsPayers],
        recipients: list[Any],
        ext_key: str | None = None,
    ) -> None:
        """Метод служит для создания сделок, в рамках которых будут проводиться выплаты via ``POST
        /v2/jsonrpc/create_deal``.

        Args:
            id: JsonRPC id
            amount: Общая сумма сделки
            ext_key: Внешний идентификатор сделки
            payers: Владельцы денег
            recipients: Получатели
        """
        return await self.execute(
            CreateDealV2(
                id=id,
                jsonrpc=jsonrpc,
                method=method,
                params=CreateDealV2Params(
                    amount=amount, ext_key=ext_key, payers=payers, recipients=recipients
                ),
            )
        )

    async def execute_deal_v2(
        self,
        id: Any,
        jsonrpc: str,
        method: str,
        deal_id: str,
        recipients_execute: list[ExecuteDealV2ParamsRecipientsExecute] | None = None,
    ) -> None:
        """Метод служит для исполнения сделки (произведения оплаты по созданной сделке) via ``POST
        /v2/jsonrpc/execute_deal``.

        Args:
            id: JsonRPC id
            deal_id: Уникальный идентификатор сделки
            recipients_execute: Список номеров получателей, по которым нужно создать платежи. Если не
                указан, то создаются платежи по всем получателям сделки
        """
        return await self.execute(
            ExecuteDealV2(
                id=id,
                jsonrpc=jsonrpc,
                method=method,
                params=ExecuteDealV2Params(deal_id=deal_id, recipients_execute=recipients_execute),
            )
        )

    async def get_deal_v2(self, id: Any, jsonrpc: str, method: str, deal_id: str) -> None:
        """Метод для получения информации по выбранной сделке via ``POST /v2/jsonrpc/get_deal``.

        Args:
            id: JsonRPC id
            deal_id: Уникальный идентификатор сделки
        """
        return await self.execute(
            GetDealV2(id=id, jsonrpc=jsonrpc, method=method, params=GetDealV2Params(deal_id=deal_id))
        )

    async def list_deal_v2(
        self,
        id: Any,
        jsonrpc: str,
        method: str,
        filters: ListDealV2ParamsFilters | None = None,
        page: int = 1,
        per_page: int = 100,
    ) -> None:
        """Метод для получения списка сделок с возможностью фильтрации via ``POST /v2/jsonrpc/list_deal``.

        Args:
            id: JsonRPC id
            filters: Фильтры
            page: Страница
            per_page: Количество элементов на странице
        """
        return await self.execute(
            ListDealV2(
                id=id,
                jsonrpc=jsonrpc,
                method=method,
                params=ListDealV2Params(filters=filters, page=page, per_page=per_page),
            )
        )

    async def list_deals_v2(
        self,
        id: Any,
        jsonrpc: str,
        method: str,
        field_names: list[ListDealsV2ParamsFieldNames] | None = None,
        filters: ListDealsV2ParamsFilters | None = None,
        page: int = 1,
        per_page: int = 100,
    ) -> None:
        """Метод для получения списка сделок с возможностью фильтрации via ``POST /v2/jsonrpc/list_deals``.

        Args:
            id: JsonRPC id
            field_names: Список возвращаемых полей
            filters: Фильтры
            page: Страница
            per_page: Количество элементов на странице
        """
        return await self.execute(
            ListDealsV2(
                id=id,
                jsonrpc=jsonrpc,
                method=method,
                params=ListDealsV2Params(
                    field_names=field_names, filters=filters, page=page, per_page=per_page
                ),
            )
        )

    async def rejected_deal_v2(self, id: Any, jsonrpc: str, method: str, deal_id: str) -> None:
        """Метод отменяет сделку, которая имеет статус new via ``POST /v2/jsonrpc/rejected_deal``.

        Args:
            id: JsonRPC id
            deal_id: Уникальный идентификатор сделки
        """
        return await self.execute(
            RejectedDealV2(
                id=id, jsonrpc=jsonrpc, method=method, params=RejectedDealV2Params(deal_id=deal_id)
            )
        )

    async def update_deal_v2(
        self, id: Any, jsonrpc: str, method: str, deal_data: UpdateDealV2ParamsDealData, deal_id: str
    ) -> None:
        """Метод для обновления информации по сделке via ``POST /v2/jsonrpc/update_deal``.

        Args:
            id: JsonRPC id
            deal_data: Изменяемые данные по сделке
            deal_id: Уникальный идентификатор сделки
        """
        return await self.execute(
            UpdateDealV2(
                id=id,
                jsonrpc=jsonrpc,
                method=method,
                params=UpdateDealV2Params(deal_data=deal_data, deal_id=deal_id),
            )
        )
